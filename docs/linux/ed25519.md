# ed25519 生成使用

```java
ssh-keygen -t ed25519 -C "your_email@example.com"
```

## git 配置
```java
git config --global user.name "dcduanchao"
git config --global user.email "111@zhidoushi.cn"

git config --global --list
```