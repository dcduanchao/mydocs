server {
    listen 80;
    listen [::]:80;

    server_name mydocs.du-ai.top;

    root /var/www/html/mydocs;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
